Method 1:Rotation


class Solution {
    int findKRotation(int arr[], int n) {
        // code here
        int a=0;
        for(int i=0;i<n-1;i++){
            if(arr[i]>arr[i+1]){
            a=i;
            return (a+1)%n;
            }
        }
        return 0;
    }
}
